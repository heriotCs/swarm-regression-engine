# BicAnnImplementation-

use these commands to run important files when in general directory :

"python3 Ann/main.py" to run ann implementation
"python3 pso/main.py" to run pso implementation
"python3 Ann/trainOnConcrete.py" to actually run the experiment on the concrete dataset. This runs predictions on the dataset using Ann which was optimised and trained by pso. 
